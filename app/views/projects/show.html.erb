<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>Owner:</strong>
  <%= User.find_by_id(@project.owner_id).name %>
</p>

<p>
  <strong>Sponsor:</strong>
  <%= User.find_by_id(@project.sponsor_id).name %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
